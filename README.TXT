
Step : ➊ ➼

      ◙ Create root folder with name Shopping cart js .
      ◙ inside it create another folder Frontend.
      ◙ then create another folder inside Frontend.
      ◙ Shopping cart js
                      |Frontend
                                | src
      ◙ Now create file index.html inside src.
      ◙ in the terminal navigate to Frontend run a command npm init. this will generate package.json file inside Frontend. 
      ◙ then run another command npm install -D live-server.
      ◙ In package.json file ther is an object with name Scripts . Copy this "start" : "live-server src -verbose" and paste this in scripts object below     "test": "echo \"Error: no test specified\" && exit 1".
      ◙ run npm start


